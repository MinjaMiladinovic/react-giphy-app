## Quick Start

```bash

1. mkdir folder_name
2. cd folder_name
3. git clone: `https://github.com/AdemHuduti/react-giphy-app.git`
4. cd react-giphy-app

# Install dependencies 
npm install

After installation run command: `npm start or yarn start`
```
